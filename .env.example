# Copy this file to ".env" and fill in your real values. Do NOT commit ".env".

# App
NODE_ENV=development
PORT=3000
PUBLIC_BASE_URL=http://localhost:3000

# Session & email (optional)
SESSION_SECRET=change-me
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password
ADMIN_NOTIFY_EMAIL=your-admin-notify@example.com

# Stripe billing
# Get your secret key from Stripe Dashboard (Developers -> API keys)
STRIPE_SECRET_KEY=sk_test_...

# Create two recurring monthly prices in Stripe Dashboard and paste their IDs
# Basic plan ($25/mo)
STRIPE_PRICE_BASIC=price_...
# Pro plan ($50/mo)
STRIPE_PRICE_PRO=price_...

# Webhook (optional in dev). If set, configure the endpoint to:
#   POST {PUBLIC_BASE_URL}/api/employer/billing/webhook
# Tip: In dev, you can leave this unset and the app will still accept JSON payloads.
STRIPE_WEBHOOK_SECRET=
# Copy this to .env and fill in real values
# Core
NODE_ENV=development
PORT=3000
SESSION_SECRET=change-this-session-secret
JWT_SECRET=change-this-jwt-secret

# Email (choose one mode)
# 1) Gmail with App Password (2FA enabled on the account)
EMAIL_USER=yourgmail@gmail.com
EMAIL_PASS=your-16-char-app-password
EMAIL_FROM=TalentSync <no-reply@talentsync.shop>
ADMIN_NOTIFY_EMAIL=your-admin@domain.com

# 2) Custom SMTP provider (uncomment to use instead of Gmail)
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# EMAIL_USER=apikey-or-username
# EMAIL_PASS=secret-or-api-key

# Optional features
ENABLE_AUTO_SCRAPING=false

# Public site base URL (used for redirects)
PUBLIC_BASE_URL=http://localhost:3000

# Billing (Stripe) — set these to enable subscriptions
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PRICE_BASIC=price_basic_25
STRIPE_PRICE_PRO=price_pro_50
STRIPE_WEBHOOK_SECRET=whsec_xxx
